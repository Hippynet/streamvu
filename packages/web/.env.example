# =============================================================================
# StreamVU Web Application Configuration
# =============================================================================
# Copy this file to .env (or .env.local for local overrides)
# All variables must be prefixed with VITE_ to be available in the browser
# =============================================================================

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
# Backend API URL
# In development: typically http://localhost:3002
# In production: your API domain (e.g., https://api.streamvu.example.com)
#
# NOTE: If not set, the app will auto-detect based on the current host:
# - In development: uses same host with port 3002
# - In production: uses same origin (assumes reverse proxy setup)
#
VITE_API_URL=http://localhost:3002

# WebSocket URL (optional, defaults to VITE_API_URL)
# Only set if your WebSocket server is on a different URL
# VITE_WS_URL=wss://ws.streamvu.example.com

# Frontend public URL (for OAuth callbacks and sharing links)
# Only needed if different from the browser's current origin
# VITE_APP_URL=https://streamvu.example.com

# -----------------------------------------------------------------------------
# Google OAuth (Optional)
# -----------------------------------------------------------------------------
# Google OAuth Client ID for "Sign in with Google" button
# Get from: https://console.cloud.google.com/apis/credentials
# VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# -----------------------------------------------------------------------------
# App Metadata
# -----------------------------------------------------------------------------
# Application version (shown in footer/about)
# VITE_APP_VERSION=1.0.0

# =============================================================================
# Development Examples
# =============================================================================
# Local development (default):
# VITE_API_URL=http://localhost:3002
#
# Network access (for testing from other devices):
# VITE_API_URL=http://192.168.1.100:3002
#
# =============================================================================
# Production Examples
# =============================================================================
# Same-origin API (behind reverse proxy):
# VITE_API_URL=https://streamvu.example.com
#
# Separate API subdomain:
# VITE_API_URL=https://api.streamvu.example.com
#
# With Google OAuth:
# VITE_API_URL=https://api.streamvu.example.com
# VITE_GOOGLE_CLIENT_ID=123456789.apps.googleusercontent.com
